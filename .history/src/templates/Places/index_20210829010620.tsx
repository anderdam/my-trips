
export type PlacesTemplateProps = {
  place: {
    slug: string;
    name: string;
    description: {
      html: string;
    }
    gallery: Ima
  }
}

export default function PlacesTemplate({place}: PlacesTemplateProps){
  return()
}
